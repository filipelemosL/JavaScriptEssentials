let x = 5;
let y = 3;

// Aritméticos
let soma = x + y;
let subtracao = x - y;
let multiplicacao = x * y;
let divisao = x / y;
let resto = x % y;


// Comparação
let igual = x === y;
let diferente = x !== y;
let maiorQue = x > y;
let menorQue = x < y;

// Lógicos
let eLogico = (x > 0) && (y < 10);
let ouLogico = (x > 0) || (y < 10);
let negacao = !(x === y);